# Analytics Lens Backstage Plugin

> [!WARNING]
> This is temporary `dev` environment for developing the Analytics Lens plugin. It is not intended for production use.
> This plugin will be submitted to [Backstage community plugins](https://github.com/backstage/community-plugins) once stable.

To start the app, run:

```sh
yarn install
yarn start
```
