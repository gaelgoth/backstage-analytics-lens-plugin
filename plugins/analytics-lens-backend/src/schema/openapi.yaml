openapi: '3.0.3'
info:
  title: analytics-lens
  description: This API powers the backend for Backstage analytics-lens plugin.
  version: '1.0'
  license:
    name: Apache-2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  contact: {}
servers:
  - url: /

paths:
  /health:
    get:
      description: Checks if the analytics-lens backend is hooked up properly
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
