backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  # database:
  #   # duplicate this block in app-config.local.yaml for postgres
  #   client: pg
  #   connection:
  #     host: ${POSTGRES_HOST}
  #     port: ${POSTGRES_PORT}
  #     user: ${POSTGRES_USER}
  #     password: ${POSTGRES_PASSWORD}
# # Very usefull for impersonate a default user

auth:
  providers:
    guest:
      userEntityRef: user:default/user-1
      dangerouslyAllowOutsideDevelopment: false